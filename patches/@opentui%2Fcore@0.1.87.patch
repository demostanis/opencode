diff --git a/index-0wbvecnk.js b/index-0wbvecnk.js
index 0c926100c..dbb236319 100644
--- a/index-0wbvecnk.js
+++ b/index-0wbvecnk.js
@@ -6370,7 +6370,7 @@ class MouseParser {
     };
     let type;
     let scrollInfo;
-    if (isScroll && pressRelease === "M") {
+    if (isScroll && !isMotion && pressRelease === "M") {
       type = "scroll";
       scrollInfo = {
         direction: scrollDirection,
@@ -6385,13 +6385,15 @@ class MouseParser {
       } else {
         type = "move";
       }
-    } else {
+    } else if (!isScroll) {
       type = pressRelease === "M" ? "down" : "up";
       if (type === "down" && button !== 3) {
         this.mouseButtonsPressed.add(button);
       } else if (type === "up") {
         this.mouseButtonsPressed.clear();
       }
+    } else {
+      return null;
     }
     return {
       type,
@@ -6415,7 +6417,7 @@ class MouseParser {
     let type;
     let actualButton;
     let scrollInfo;
-    if (isScroll) {
+    if (isScroll && !isMotion) {
       type = "scroll";
       actualButton = 0;
       scrollInfo = {
@@ -6423,11 +6425,21 @@ class MouseParser {
         delta: 1
       };
     } else if (isMotion) {
-      type = "move";
+      type = button === 3 || this.mouseButtonsPressed.size === 0 ? "move" : "drag";
       actualButton = button === 3 ? -1 : button;
-    } else {
+      if (button === 3) {
+        this.mouseButtonsPressed.clear();
+      }
+    } else if (!isScroll) {
       type = button === 3 ? "up" : "down";
       actualButton = button === 3 ? 0 : button;
+      if (type === "down" && button !== 3) {
+        this.mouseButtonsPressed.add(button);
+      } else if (type === "up") {
+        this.mouseButtonsPressed.clear();
+      }
+    } else {
+      return null;
     }
     return {
       type,
diff --git a/index.js b/index.js
index 1970eef38..725b6e15e 100644
--- a/index.js
+++ b/index.js
@@ -9817,19 +9817,19 @@ class SliderRenderable extends Renderable {
   }
   setupMouseHandling() {
     let isDragging = false;
+    let pending = false;
     let dragOffsetVirtual = 0;
     this.onMouseDown = (event) => {
       event.stopPropagation();
       event.preventDefault();
+      pending = false;
       const thumb = this.getThumbRect();
       const inThumb = event.x >= thumb.x && event.x < thumb.x + thumb.width && event.y >= thumb.y && event.y < thumb.y + thumb.height;
       if (inThumb) {
         isDragging = true;
         dragOffsetVirtual = this.calculateDragOffsetVirtual(event);
       } else {
-        this.updateValueFromMouseDirect(event);
-        isDragging = true;
-        dragOffsetVirtual = this.calculateDragOffsetVirtual(event);
+        pending = true;
       }
     };
     this.onMouseDrag = (event) => {
@@ -9839,10 +9839,13 @@ class SliderRenderable extends Renderable {
       this.updateValueFromMouseWithOffset(event, dragOffsetVirtual);
     };
     this.onMouseUp = (event) => {
-      if (isDragging) {
+      if (pending) {
+        this.updateValueFromMouseDirect(event);
+      } else if (isDragging) {
         this.updateValueFromMouseWithOffset(event, dragOffsetVirtual);
       }
       isDragging = false;
+      pending = false;
     };
   }
   updateValueFromMouseDirect(event) {
